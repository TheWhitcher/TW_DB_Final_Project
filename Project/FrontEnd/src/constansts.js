const constants = {
    BACKEND_URL: '' //Front end inside back end
    //BACKEND_URL: 'https://25c7-205-236-4-254.ngrok-free.app', //NGROK *update for every new connection*
    //BACKEND_URL: 'http://localhost:8080' //Localhost
}

// Start the ngrok:
// ngrok start --all --config ngrok.yml

export default constants;