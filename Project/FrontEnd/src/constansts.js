const constants = {
    //BACKEND_URL: ' https://5455-205-236-4-254.ngrok-free.app', //NGROK *update for every new connection*
    BACKEND_URL: 'http://localhost:8080' //Localhost
}

// Start the ngrok:
// ngrok start --all --config ngrok.yml

export default constants;